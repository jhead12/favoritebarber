// TODO: implement geocoding worker
// Responsibilities:
// - Resolve addresses to lat/lng using provider (Mapbox, Google, or Nominatim)
// - Cache responses in `geocoding_cache`

export async function geocodeAddress(address: string) {
  console.log('geocodeAddress', address);
}
